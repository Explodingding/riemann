[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18.19.1"

# Przekierowania dla Single Page Application
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 